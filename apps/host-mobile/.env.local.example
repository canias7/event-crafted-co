# Copy to .env.local and fill in. Both vars are also set as EAS Secrets
# (eas secret:create) so production builds pick them up at build time.
EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-publishable-anon-key
